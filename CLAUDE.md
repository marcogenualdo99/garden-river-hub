<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-06-20 — do not edit manually*

### File Structure
*~15,000 tokens/session saved*
- `index.html` is ~70KB; always use `limit` and `offset` params when reading it — never load the full file in one call unless absolutely necessary.

### Environment
*~500 tokens/session saved*
- `gh` CLI is NOT installed on this machine; never use `gh api` or any `gh` subcommand — use `WebFetch` or `curl` for GitHub API calls instead.
- `node`, `npx`, and `npm` are NOT installed; do not attempt `npm install` or `npx` commands.
- `/usr/local/bin/` does not exist; search for binaries in `/usr/bin/` instead. `/usr/bin/python3` (3.9) is available.

### Skills
*~500 tokens/session saved*
- Installed skills live in `/Users/marcogenualdo/Library/Application Support/Claude/local-agent-mode-sessions/skills-plugin/`; do not search for this directory with `ls` or `find`.

<!-- headroom:learn:end -->
